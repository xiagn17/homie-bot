apiVersion: v1
kind: Service
metadata:
  name: homie-service
  labels:
    name: homie-api
    env: prod
spec:
  type: NodePort
  selector:
    app: homie-api
  ports:
    - name: app-listener
      protocol: TCP
      port: 80
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: homie-postgresql
spec:
  selector:
    app: postgresql-db
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
