apiVersion: v1
kind: Service
metadata:
  name: homie-service
  labels:
    env: prod
spec:
  type: LoadBalancer
  selector:
    app: homie-api
  ports:
    - name: app-listener
      protocol: TCP
      port: 80
      targetPort: 3000
