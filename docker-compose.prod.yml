version: '3.7'

services:
  backend:
    container_name: backend_prod
    build:
      context: .
      target: production
      dockerfile: ./Dockerfile
    command: npm run start:prod
    restart: always
